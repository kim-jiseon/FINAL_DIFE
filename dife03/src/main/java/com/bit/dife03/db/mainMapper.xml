<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main">
<select id="sel_board" resultType="boardVo">
	select * from board where boa_sort like '%후기%' and rownum <![CDATA[<=]]> 4  order by boa_view desc
</select>
</mapper>